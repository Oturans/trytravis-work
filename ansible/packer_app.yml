---
- name: Install Ruby && Bundler
  hosts: app
  become: true
  tasks:
  # Установим в цикле все зависимости
  - name: Install ruby 
    apt: 
      name: "{{ item }}"
      state: present
    with_items:
      - "ruby-full"
      - "ruby-bundler"
      - "build-essential"
